## Auto-Commit Rule
Always push changes to GitHub after completing a user request or significant update. Wait for user confirmation ONLY if explicitly requested or if there's a risk of breaking something, otherwise run `git add .`, `git commit`, and `git push` autonomously.
